command:
  - streamlit
  - run
  - project/app_streamlit.py
  - --server.address=0.0.0.0
  - --server.port=8000

secrets:
  - scope: databricks-evaluation-app
    key: DATABRICKS_HOST
    env_var: DATABRICKS_HOST
  - scope: agent-obo-scope
    key: obo-token
    env_var: DATABRICKS_OBO_TOKEN
  # Map the same OBO token to DATABRICKS_TOKEN so the Databricks SDK auth
  # chain (used by ChatDatabricks / langchain-databricks) also picks it up.
  - scope: agent-obo-scope
    key: obo-token
    env_var: DATABRICKS_TOKEN

env:
  - name: PYTHONPATH
    value: .
  - name: LLM_PROVIDER
    value: databricks
  - name: DATABRICKS_MODEL_ENDPOINT
    value: databricks-meta-llama-3-3-70b-instruct
